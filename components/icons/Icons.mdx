---
name: Icons
menu: Components
---

import { Playground, Props } from 'docz'
import {
	EMAIL,
	GITLAB,
	LINKEDIN,
	NAME,
	TWITTER,
} from '../../config/personalDetails'
import Blog from './Blog'
import Gitlab from './Gitlab'
import LinkedIn from './LinkedIn'
import Mail from './Mail'
import PDF from './PDF'
import Twitter from './Twitter'
import Reddit from './Reddit'

# Icons

## Properties

All icons accept these properties

<Props of={Gitlab} />

Except Blog because it uses the Next.js Link thus must refer to the integrated blog

<Props of={Blog} />

## Basic usage

<Playground>
	<Gitlab href={`https://${GITLAB}`} label="GitLab" />
	<LinkedIn href={`https://${LINKEDIN}`} label="LinkedIn" />
	<Twitter href={`https://${TWITTER}`} label="Twitter" />
	<Mail href={`mailto:${EMAIL}`} label="Mail" />
	<PDF href="/files/EmmaJCline-resume.pdf" label="PDF" />
	<Reddit href="https://www.reddit.com/user/EmmaJCline" label="Reddit" />
	<Blog />
</Playground>
